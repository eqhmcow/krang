<tmpl_include header.tmpl>
<tmpl_unless bug_added>

<tmpl_if ise>
  <div class=alert>An internal server error has occurred.  This almost certainly indicates a bug in the system.  <tmpl_if enable_bugzilla>Please use the form below to file a bug report.<tmpl_else>Please contact the administrator.</tmpl_if> </div><p>
</tmpl_if>

<tmpl_if enable_bugzilla>

<div class="dark-bar"><p class="left">&nbsp;Submit a Bug</p></div>
<form name="bug_form" method="post" action="bug.cgi">
<div class="box"><br>
<table cellspacing="0" width="571" class="form-cell">
<tr>
<td class="form-cell">*<b>Summary</b> </td><td class="form-cell"><input type="text" size="50" name="summary" value="<tmpl_var escape=HTML summary>" <tmpl_if no_summary>class="invalid"</tmpl_if>><p>
        A sentence which summarizes the problem.
        Please be descriptive and use lots of keywords.
      </p> 
<p>
        <tt>
          <font color="#990000">Bad example</font>: it crashed when in story.
        </tt>

       <br>
        <tt>
          <font color="#009900">Good example</font>:
          it crashed with an internal server error while I was editing story "my story", after adding a contributor and clicking 'save'.
        </tt>
      </p>
    </td> 
</tr>
<tr>
    <td class="form-cell">
      *<b>Description</b> 
    </td>
    <td class="form-cell">
<textarea rows="6" cols="50" name="description" value="<tmpl_var escape=HTML description>" <tmpl_if no_description>class="invalid"</tmpl_if>
                wrap="hard"></textarea>
      <p>
        Expand on the Summary. Please be
        as specific as possible about what is wrong.
      </p>
      <p>

        <tt>
          <font color="#990000">Bad example</font>: Covers won't publish!!!
        </tt>
       <br>
        <tt>
          <font color="#009900">Good example</font>: When I tried to publish the cover for the news/sports category from the edit story screen I got an internal server error.
        </tt>
      </p>
    </td>
  </tr>

<tr>
    <td class="form-cell">
      <b>Reproducability</b>
    </td>
    <td class="form-cell">
<select name="reproduce">
        <option name="Always" value="Always" <tmpl_if reproduce_Always>selected</tmpl_if>>
          Every time.
        </option>
        <option name="Sometimes" value="Sometimes" <tmpl_if reproduce_Sometimes>selected</tmpl_if>>
          Sometimes, but not always.
        </option>
        <option name="DidntTry" value="DidntTry" <tmpl_if reproduce_DidntTry>selected</tmpl_if>>
                                                                           
          Haven't tried to reproduce it.
        </option>
        <option name="NotReproducible" value="CouldNotReproduce" <tmpl_if reproduce_CouldNotReproduce>selected</tmpl_if>>
          Tried, but couldn't reproduce it.
        </option>
      </select>
      <p>
        How often can you reproduce the problem?
      </p>
    </td>
  </tr>

<tr>

    <td class="form-cell">
      <b>Steps to Reproduce</b> 
    </td>
    <td class="form-cell">
<textarea rows="4" cols="50" name="steps" wrap="hard">
<tmpl_if steps><tmpl_var escape=HTML steps><tmpl_else>1.
2.
3.</tmpl_if>
</textarea>
      <p>
        Describe how to reproduce the problem, step by
        step. Include any special setup steps.
      </p>
    </td>

  </tr>

  <tr> 
    <td class="form-cell">
      <b>Actual Results</b> 
    </td>
<td class="form-cell">
<textarea rows="4" cols="50" name="actual_results"
                wrap="hard"><tmpl_var escape=HTML actual_results></textarea>
      <p>
        What happened after you performed the steps above?
      </p>  
    </td>
  </tr>

  <tr>
    <td class="form-cell">
      <b>Expected Results</b> 
    </td>
    <td class="form-cell">
<textarea rows="4" cols="50" name="expected_results"
                wrap="hard"><tmpl_var escape=HTML expected_results></textarea>
      <p>

        What should the system have done instead?
      </p>
    </td>
  </tr>
  <tr>
    <td class="form-cell">
      <b>Severity</b>
    </td>
    <td class="form-cell">
      <select name="bug_severity" style="width:420px;">
        <option name="normal" value="normal" <tmpl_if bug_severity_normal>selected</tmpl_if>>
          Normal: It's a bug that should be fixed.
        </option>
        <option name="blocker" value="blocker" <tmpl_if bug_severity_blocker>selected</tmpl_if>>
          Blocker: This bug prevents you from using the system.
        </option>
        <option name="critical" value="critical" <tmpl_if bug_severity_critical>selected</tmpl_if>>
          Critical: the system crashes, hangs, or causes you to 
          lose data.
        </option>
        <option name="major" value="major" <tmpl_if bug_severity_major>selected</tmpl_if>>
          Major: A major feature is broken.
        </option>
        <option name="minor" value="minor" <tmpl_if bug_severity_minor>selected</tmpl_if>>
          Minor: Minor loss of function, and there's an easy workaround.
        </option>
        <option name="trivial" value="trivial" <tmpl_if bug_severity_blocker>trivial</tmpl_if>>
          Trivial: A cosmetic problem, such as a misspelled word or 
          misaligned text.
        </option>
      </select>
    <p>
        How serious is the problem?
      </p>

    </td>
  </tr>
</table><br>
</div>
<input type="hidden" name="rm" value="commit" override='1'>
<div class=button-bar>
  <input type=submit value="Submit Bug" class="button">
</div>
</form>

</tmpl_if>
</tmpl_unless>
</div>

<tmpl_include footer.tmpl>
